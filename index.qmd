---
title: "Canopy to Cohort"
subtitle: "Detecting exploratory sugar maple regeneration gaps in Michigan northern hardwood forests"
description: "A reproducible R analysis of sugar maple regeneration gaps in Michigan northern hardwood forests using FIA, Daymet, GIS, and mixed-effects modeling."
image: "outputs/figures/03_regeneration_quadrant.png"
page-layout: full
title-block-banner: true
format:
  html:
    toc: false
    code-tools: false
execute:
  echo: false
  warning: false
  message: false
---

```{r}
#| label: setup
summary <- readr::read_csv(here::here("outputs", "tables", "analysis-summary.csv"), show_col_types = FALSE)
support <- readr::read_csv(here::here("outputs", "tables", "model-support.csv"), show_col_types = FALSE)
effect_curve <- readr::read_csv(here::here("outputs", "tables", "model-maple-effect-curve.csv"), show_col_types = FALSE)
validation <- readr::read_csv(here::here("outputs", "tables", "model-cross-validation-summary.csv"), show_col_types = FALSE)
evaluation <- readr::read_csv(here::here("outputs", "audits", "selected-evaluation.csv"), show_col_types = FALSE)
fia_counts <- readr::read_csv(here::here("outputs", "audits", "fia-table-counts.csv"), show_col_types = FALSE)
cohort <- readr::read_csv(here::here("outputs", "tables", "cohort-flow.csv"), show_col_types = FALSE)
curve_probability <- function(x) {
  stats::approx(effect_curve$maple_ba_ft2_ac, effect_curve$predicted_probability, xout = x, rule = 2)$y
}
cv_metric <- function(metric_name) {
  value <- validation$value[validation$metric == metric_name]
  if (length(value) != 1L) stop("Required validation metric not found: ", metric_name)
  value
}
table_rows <- function(table_name) {
  value <- fia_counts$rows[fia_counts$table == table_name]
  if (length(value) != 1L) stop("Required FIA table count not found: ", table_name)
  value
}
cohort_rows <- function(step_name) {
  value <- cohort$observations[cohort$step == step_name]
  if (length(value) != 1L) stop("Required cohort step not found: ", step_name)
  value
}
```

:::: {.columns .landing-hero}
::: {.column width="46%"}
## A forest can keep its canopy while losing its next cohort

This project asks where contemporary Michigan FIA observations combine substantial established sugar maple with no sugar-maple seedlings tallied, and which measured stand and broad climate characteristics accompany that mismatch.

[Read the research report](report/index.qmd){.btn .btn-primary .btn-lg}
[Review analysis decisions](docs/analysis-decisions.md){.btn .btn-outline-secondary .btn-lg}
:::

::: {.column width="54%"}
![Established sugar-maple basal-area share versus observed seedling density.](outputs/figures/03_regeneration_quadrant.png){fig-alt="Scatterplot of sugar maple basal-area share against seedling density, highlighting sampled conditions that meet the exploratory regeneration-gap screen."}
:::
::::

::: {.builder-strip}
**What I built:** reference-driven FIA extraction · audited relational joins · county-safe Daymet integration · mixed-effects modeling · sensitivity analysis · publication-ready reporting
:::

## The study at a glance

::::: {.stat-grid}
:::: {.stat-card}
::: {.stat-number}
`r scales::comma(summary$plot_conditions)`
:::
Eligible plot-condition measurements
::::

:::: {.stat-card}
::: {.stat-number}
`r scales::comma(summary$seedling_sampled)`
:::
Conditions with seedling sampling
::::

:::: {.stat-card}
::: {.stat-number}
`r scales::comma(summary$potential_gaps)`
:::
Exploratory potential gaps
::::

:::: {.stat-card}
::: {.stat-number}
`r summary$counties`
:::
Michigan counties represented
::::
:::::

## From inventory database to research artifact

:::::: {.workflow-grid}
::::: {.workflow-card}
[01 · Inspect]{.workflow-index}

### Resolve the source

`r scales::label_number(scale_cut = scales::cut_short_scale())(table_rows("TREE"))` TREE and `r scales::label_number(scale_cut = scales::cut_short_scale())(table_rows("SEEDLING"))` SEEDLING rows are profiled alongside FIA evaluation and reference tables.
:::::

::::: {.workflow-card}
[02 · Define]{.workflow-index}

### Build the cohort

`r scales::comma(cohort_rows("Plot visits assigned to current evaluation"))` assigned visits narrow to `r scales::comma(summary$plot_conditions)` conditions in FIA's maple/beech/birch forest-type group, used as the operational northern-hardwood cohort.
:::::

::::: {.workflow-card}
[03 · Integrate]{.workflow-index}

### Preserve geography

83 county Daymet cells join by GEOID. Exact FIA plot locations are confidential; public coordinates may be approximate, and this analysis neither uses nor infers them.
:::::

::::: {.workflow-card}
[04 · Estimate]{.workflow-index}

### Model clustering

`r scales::comma(support$model_complete_n)` outcomes support a county random intercept and a nonlinear established-maple response.
:::::

::::: {.workflow-card}
[05 · Validate]{.workflow-index}

### Publish evidence

County-held-out validation (Brier `r sprintf("%.3f", cv_metric("Brier score"))`; AUC `r sprintf("%.3f", cv_metric("ROC AUC"))`), join audits, source hashes, diagnostics, and threshold sensitivity accompany the report.
:::::
::::::

::: {.reviewer-strip}
**Technical reviewer?** [Inspect the data dictionary](docs/data-dictionary.md) · [Trace analysis decisions](docs/analysis-decisions.md) · [Review the source and validation evidence](https://github.com/dineshpotla/canopy-to-cohort)
:::

## What the analysis found

Sugar-maple seedlings were tallied in **`r scales::percent(summary$maple_seedling_detected / summary$seedling_sampled, accuracy = 0.1)`** of sampled conditions. The upper-third screen flagged **`r scales::percent(summary$potential_gap_fraction, accuracy = 0.1)`** as potential gaps. In the adjusted `r support$model_type`, the established-maple relationship was nonlinear: at the stated reference profile, predicted non-detection fell from **`r scales::percent(curve_probability(0), accuracy = 1)` at 0 ft²/acre** to **`r scales::percent(curve_probability(25), accuracy = 1)` at 25 ft²/acre**, then leveled off.

That is an association, not a causal effect. The screen is sample-relative, a seedling zero means “not tallied” rather than confirmed ecological absence, and county climate values are broad proxies.

:::: {.columns .feature-row}
::: {.column width="50%"}
### Built for auditability

- FIA species and forest types resolve from reference tables.
- Trees and seedlings aggregate before joining.
- Sampling opportunity controls whether a missing seedling record can become zero.
- Checksums, join audits, support gates, and tests travel with the analysis.
:::

::: {.column width="50%"}
### Spatially responsible

Exact FIA plot locations are confidential. The analysis neither uses nor infers public plot coordinates; maps and climate joins use county identifiers, and sparse county summaries are suppressed.
:::
::::

## Research artifact

The report documents the pinned 2025 current evaluation (inventory window `r evaluation$start_invyr`–`r evaluation$end_invyr`; assigned measurement records `r summary$years_min`–`r summary$years_max`), cohort construction, equations, model support, sensitivity analysis, limitations, and reproducibility workflow.

[Open the complete report →](report/index.qmd){.report-link}
